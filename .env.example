# =============================================
# SpendWise - Environment Variables (EXAMPLE)
# =============================================
# Copie este arquivo para .env e preencha com seus valores

# =====================
# Database (PostgreSQL)
# =====================
# Para desenvolvimento local, use:
# DB_HOST=localhost
# DB_PORT=5432
# DB_NAME=appdb
# DB_USER=appuser
# DB_PASSWORD=apppassword
# DB_SSL_MODE=Disable

# Para Supabase/Render (produção):
DB_HOST=seu-host.supabase.co
DB_PORT=5432
DB_NAME=postgres
DB_USER=postgres
DB_PASSWORD=sua-senha-segura
DB_SSL_MODE=Require

# =====================
# JWT Authentication
# =====================
# IMPORTANTE: Gere uma chave forte de 64+ caracteres
# PowerShell: -join ((65..90) + (97..122) + (48..57) | Get-Random -Count 64 | % {[char]$_})
# Bash: openssl rand -base64 64
JWT_SECRET=sua-chave-secreta-super-segura-minimo-64-caracteres
JWT_ISSUER=SpendWise
JWT_AUDIENCE=SpendWise
JWT_EXPIRATION=60

# =====================
# CORS (Frontend URLs)
# =====================
# Desenvolvimento:
# CORS_ALLOWED_ORIGINS=http://localhost:3000

# Produção (separe múltiplas URLs com vírgula):
CORS_ALLOWED_ORIGINS=https://seu-frontend.vercel.app,https://seu-frontend.onrender.com

# =====================
# Email Service (Opcional)
# =====================
# Para produção, configure um serviço de email real
# BREVO_API_KEY=sua-chave-brevo
# EMAIL_FROM=noreply@spendwise.com
# EMAIL_FROM_NAME=SpendWise

# =====================
# Application Settings
# =====================
ASPNETCORE_ENVIRONMENT=Production
NODE_ENV=production
